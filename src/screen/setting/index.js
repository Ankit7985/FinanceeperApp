import { View, Text,TextInput } from 'react-native'
import React from 'react'
import CustomHeader from '../../components/customHeader'
import { useState } from 'react'

const Setting=()=> {

  return (
    <View style={{flex:1}}>
    <CustomHeader headerTitle={'Setting'} />
   
    </View>
  )
}
export default Setting